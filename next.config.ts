import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Disable React Strict Mode to prevent AbortError during development */
  reactStrictMode: false,
};

export default nextConfig;
